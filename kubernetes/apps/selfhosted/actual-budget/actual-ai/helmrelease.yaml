---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: actual-ai
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 4.5.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s-labs
        namespace: flux-system
  dependsOn:
    - name: actual-budget
      namespace: selfhosted
  values:
    controllers:
      actual-ai:
        annotations:
          secret.reloader.stakater.com/reload: actual-ai-secret

        pod:
          labels:
            egress.home.arpa/internet: allow
            egress.home.arpa/kubedns: allow # FIXME: Remove when clusterNetworkPolicy is in place
            egress.home.arpa/actual-budget: allow
          securityContext:
            runAsUser: "${APP_UID}"
            runAsGroup: "${APP_GID}"
            fsGroup: "${APP_GID}"
            runAsNonRoot: true

        containers:
          app:
            image:
              repository: sakowicz/actual-ai
              tag: 2.2.4@sha256:15c65d204528feaf30dbde07c391ec097a529d5fb9bf1c4b11b1e8f349e0c1c7
              pullPolicy: IfNotPresent

            envFrom:
              - secretRef:
                  name: actual-ai-secret

            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false

            resources:
              requests:
                cpu: 50m
                memory: 256Mi
              limits:
                memory: 512Mi

            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities:
                drop:
                  - ALL

    # No service needed as this is a cron-based application
    service:
      app:
        controller: actual-ai
        enabled: false
