---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Add Silence resources in this directory as needed. Keeping the Kustomization
# ensures Flux applies future silences without additional wiring.
