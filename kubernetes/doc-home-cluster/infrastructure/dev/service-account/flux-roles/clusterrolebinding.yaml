apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: flux-api-discovery-binding
subjects:
  - kind: ServiceAccount
    name: flux-reconcile-sa
    namespace: dev
roleRef:
  kind: ClusterRole
  name: system:discovery
  apiGroup: rbac.authorization.k8s.io