---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: alertmanager
  namespace: monitoring
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controller:
      type: statefulset
      annotations:
        reloader.stakater.com/auto: "true"

    image:
      repository: quay.io/prometheus/alertmanager
      tag: main@sha256:a40b0f4839bb5c49504056a16b529818f5150d3e3d0dbd5c105c46600e996aa8

    podAnnotations:
      reloader.stakater.com/auto: "true"

    service:
      main:
        ports:
          http:
            port: 9093

    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        hosts:
          - host: &host alertmanager.piscio.net
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host

    persistence:
      config:
        enabled: true
        type: secret
        mountPath: /etc/alertmanager
        name: alertmanager-config
        items:
        - key: alertmanager.yml
          path: alertmanager.yml

    
    #   config:
    #     enabled: true
    #     type: configMap
    #     name: alertmanager-configmap
    #     mountPath: /etc/alertmanager
    #     readOnly: true
    
    # envFrom:
    #   - secretRef:
    #       name: alertmanager-secret

    resources:
      requests:
        cpu: 11m
        memory: 50M
      limits:
        memory: 99M
