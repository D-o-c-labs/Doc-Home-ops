---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: oauth2-proxy-redis
  namespace: security
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.0.3
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    controller:
      strategy: RollingUpdate

    image:
      repository: public.ecr.aws/docker/library/redis
      tag: 7.2.3@sha256:31f2ab93065d238a25dbcfc7feb00f419cdfd66efefd40d066924227567147c8

    podAnnotations:
      secret.reloader.stakater.com/reload: oauth2-proxy-redis-secrets

    command: ["redis-server", "--requirepass", "$(REDIS_PASSWORD)"]

    env:
      REDIS_REPLICATION_MODE: master

    envFrom:
      - secretRef:
          name: oauth2-proxy-redis-secrets

    service:
      main:
        ports:
          http:
            enabled: false
          redis:
            enabled: true
            port: 6379

    resources:
      requests:
        cpu: 15m
        memory: 64M
      limits:
        memory: 128M
