---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: flaresolverr
  namespace: downloads
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system

  values:
    controller:
      type: statefulset
      annotations:
        reloader.stakater.com/auto: "true"

    image:
      repository: ghcr.io/flaresolverr/flaresolverr
      tag: v3.2.2@sha256:b1f154151a2c466cb19f5cf0dd2d1ba712240f6ad5e0ba27acca462bf00c2446

    env:
      TZ: Europe/Rome

    service:
      main:
        ports:
          http:
            port: 8191


    resources:
      requests:
        cpu: 250m
        memory: "1Gi"
      # limits:
      #   memory: "4096M"
